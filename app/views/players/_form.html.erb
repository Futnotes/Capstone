<%= form_for(@player, :url => players_path) do |f| %>
  
    <%= f.hidden_field :team_id, value: params[:team_id] %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit "Save" %>

<% end %>
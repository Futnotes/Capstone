<%= form_for @match do |f| %>

  <%= f.hidden_field :team_id, value: params[:team_id] %>
  
  <%= f.label :team_two_name %>
  <%= f.text_field :team_two_name %>

  <%= f.label :kick_off %>
  <%= f.date_select :kick_off %>
  
  <%= f.submit "Save" %>
<% end %>